const tabs = (param) => {
  const { selectorTabsBtn, activeClassBtn, selectorTabsItem, activeClassTab } = param;

  console.log(selectorTabsBtn, activeClassBtn, selectorTabsItem, activeClassTab);
};

export default tabs;
